{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --include=dev --prefix backend && npm run prisma:generate --prefix backend",
    "start": "npm run prisma:migrate:deploy --prefix backend && npm start --prefix backend"
  }
}
